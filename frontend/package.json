{
  "name": "market-traits-frontend",
  "version": "0.1.0",
  "description": "Shared React data hooks + TypeScript types for the market-traits package's API responses (market_weather / global_markets / industry_markets).",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build"
  },
  "peerDependencies": {
    "react": ">=18"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/react": "^18.3.0"
  }
}
